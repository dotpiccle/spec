# Non-normative numeric aids

These values help engine authors catch transcription and boundary mistakes without embedding a reference engine or PCM output. The normative formulas remain in `docs/`; these aids alone do not prove audible conformance.

Although these aids are labeled non-normative (they can be regenerated from the formulas in `docs/`), they are **normatively referenced** by the conformance gates in [docs/15-engine-build-guide.md](../../docs/15-engine-build-guide.md): a conforming engine MUST recompute and compare every entry. All entries must match exactly, except `lowpass_1000_hz_48000_resonance_0` (subject to platform `sin`/`cos` variance — verify via the spectral purity test in `docs/15` step 4 instead). If any value disagrees with the formulas in `docs/`, the formulas are authoritative; see [docs/14-conformance.md](../../docs/14-conformance.md) §Role of repository fixtures.

`dsp-values.json` covers PCG32 initialization, transition-curve checkpoints, oscillator Fourier coefficients and DFT convention, a canonical biquad coefficient set, equal-power balance and mono adaptation, absolute timing boundaries, pitch transformation order, canonical mixing order, render-profile frequency clamping, and reverb tail boundaries, terminal windows, and baseline generator configuration. `reverb-matrix-vector.json` contains a test vector for the random orthogonal feedback matrix construction (seed, PCG32 outputs, source matrix, and resulting Q for a non-canonical configuration). `reverb-qualification-matrix.json` contains the finite mandatory qualification matrix for reverb conformance: 10 non-canonical `(tail_ms, soften_hz, sample_rate)` configurations covering edge cases (minimum/maximum parameter values, Nyquist clamping, rounding boundaries, long FFT sizing) that an engine MUST verify against the normative FDN generator (see [docs/15-engine-build-guide.md](../../docs/15-engine-build-guide.md) step 6). `reverb-reference-irs/` contains the canonical reference IR render fixtures for the reverb perceptual-equivalence gate.
